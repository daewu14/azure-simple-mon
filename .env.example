# Server
PORT=5762
HOST=0.0.0.0

# Azure DevOps
AZURE_DEVOPS_ORG=KiriminAja2026
AZURE_DEVOPS_PROJECT=Product Delivery
AZURE_DEVOPS_TEAM=Platform Squad
# Optional comma-separated list, e.g. "Platform Squad,Backend Squad"
AZURE_DEVOPS_TEAMS=
# Required for live Azure DevOps data. Keep secret.
AZURE_DEVOPS_PAT=

# Dashboard login
DASHBOARD_AUTH_USERNAME=daewubintara@kiriminaja.com
# Generate salt and hash with:
# node -e "const crypto=require('crypto'); const salt=crypto.randomBytes(16).toString('hex'); const password='CHANGE_ME'; console.log({salt, hash:crypto.createHash('sha256').update(`${salt}:${password}`).digest('hex')})"
DASHBOARD_AUTH_SALT=
DASHBOARD_AUTH_PASSWORD_SHA256=
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
DASHBOARD_SESSION_SECRET=
